import React from "react";  

const ComponentPage = () => {
    if (typeof window !== `undefined`) window.location.replace(`/components/cards/single-item`)
    return <></>
}

export default ComponentPage